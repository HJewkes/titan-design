---
description: Set up or modify CI pipeline for this Turborepo monorepo
user_invocable: true
---

# CI Setup

This file is a pointer. Follow _CI and scripts_ in the repo's root `CLAUDE.md` (the steps CI runs,
Turbo argument passthrough, script registration, coverage thresholds). The workflow files in
`.github/workflows/` are the source of truth.
