---
description: Add a new design token to the token system (all 4 required files)
user_invocable: true
---

# Add New Design Token

This file is a pointer. Follow _Design Tokens_ → _Adding New Tokens_ in the repo's root
`CLAUDE.md`. It covers the four-file chain, the hand-kept `theme/config.ts` mirror, and the
separate route for spacing and sizing tokens. `packages/ui/TOKENS.md` covers which token a
component should use.
